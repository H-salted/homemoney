# 家智享 - 智能家庭管理系统

一个现代化的家庭管理Web应用，提供美食决策、AI智能账本、预算管理等功能。

## 🚀 部署到Netlify

### 方法一：通过Git仓库部署

1. 将代码推送到GitHub/GitLab/Bitbucket
2. 在Netlify中连接你的仓库
3. 设置构建配置：
   - **Build command**: 留空
   - **Publish directory**: `.` (根目录)
   - **Node version**: `18`

### 方法二：拖拽部署

1. 运行 `npm run build` (如果需要)
2. 将整个项目文件夹拖拽到Netlify部署页面

## 📁 项目结构

```
├── index.html          # 主入口页面（登录/注册）
├── login.html          # 登录页面
├── register.html       # 注册页面
├── dashboard.html      # 用户仪表板
├── food-wheel.html     # 美食决策机
├── ai-ledger.html      # AI智能账本
├── budget-management.html # 预算管理
├── settings.html       # 个人设置
├── assets/             # 静态资源
│   ├── font-awesome.min.css  # Font Awesome图标库
│   └── supabase-js.min.js    # Supabase客户端库
├── scripts/            # JavaScript文件
│   ├── auth-check.js   # 登录验证
│   ├── common.js       # 通用功能
│   ├── theme.js        # 主题切换
│   ├── food-wheel.js   # 美食决策功能
│   ├── ai-ledger.js    # AI账本功能
│   ├── budget-management.js # 预算管理功能
│   ├── flatpickr.min.js # 日期选择器
│   └── zh.js           # 中文本地化
├── styles/             # CSS样式文件
│   ├── common.css      # 通用样式
│   ├── theme.css       # 主题样式
│   ├── settings.css    # 设置页面样式
│   ├── ai-ledger.css   # AI账本样式
│   └── flatpickr.min.css # 日期选择器样式
├── netlify.toml        # Netlify配置文件
└── _redirects          # URL重定向规则
```

## 🔧 配置说明

### 环境变量
在Netlify中设置以下环境变量（如果需要）：
- `SUPABASE_URL`: Supabase数据库URL
- `SUPABASE_ANON_KEY`: Supabase匿名密钥

### 重定向规则
项目已配置 `_redirects` 文件处理客户端路由，所有未匹配的路径都会重定向到 `index.html`。

### 安全头
已在 `netlify.toml` 中配置安全HTTP头：
- X-Frame-Options: DENY
- X-XSS-Protection: 1; mode=block
- X-Content-Type-Options: nosniff

## 🌐 功能特性

- ✅ 响应式设计，支持移动端
- ✅ 用户认证系统
- ✅ 美食决策机
- ✅ AI智能账本
- ✅ 预算管理
- ✅ 个人设置
- ✅ 主题切换
- ✅ 离线数据存储

## 🛠️ 技术栈

- **前端**: HTML5, CSS3, JavaScript (ES6+)
- **样式**: CSS Grid, Flexbox, CSS动画
- **图标**: Font Awesome
- **数据库**: Supabase
- **部署**: Netlify

## 📱 浏览器支持

- Chrome 60+
- Firefox 55+
- Safari 12+
- Edge 79+

## 🤝 贡献

欢迎提交Issue和Pull Request来改进这个项目。

## 📄 许可证

MIT License